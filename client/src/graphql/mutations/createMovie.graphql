mutation CreateMovie($title: String! $minutes: Int!) {
  createMovie(options: { title: $title, minutes: $minutes }) {
    errors {
      field
      message
    }
    movie {
      id
      title
      minutes
      createdAt
      updatedAt
    }
  }
}
