query Movie($id: String!) {
  movie(id: $id) {
    id
    title
    url
    minutes
    createdAt
    updatedAt
    comments {
      id
      content
      createdAt
      updatedAt
      user {
        email
      } 
    }
  }
}
